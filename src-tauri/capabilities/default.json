{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Boot screen in the main window, before dsh is serving",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "allow-dsh-status",
    "allow-restart-dsh"
  ]
}
